/**
 * Blur layers are mounted once in `TemplateRuntimeProvider` via `BlurScrollRoot`.
 * Use `BlurSection` from `@/components/template/blur/BlurSection` around content
 * that should drive the overlay (replaces `.blur-section` + `mxdBlur`).
 */
export default function BlurEffect() {
  return null;
}

